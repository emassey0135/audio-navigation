{
 "id": "${mod_id}",
 "name": "${mod_name}",
 "version": "${version}",
 "description": "${mod_description}",
 "authors": ["${mod_author}"],
  "dependencies": {
    "minecraft": ">=${minecraft_version}"
  }
}

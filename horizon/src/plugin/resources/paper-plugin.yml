name: ${mod_name}
version: ${version}
main: dev.emassey0135.audionavigation.paper.AudioNavigationPaper
description: ${mod_description}
author: ${mod_author}
website: ${mod_github_url}
api-version: ${paper_api_version}
load: STARTUP
prefix: ${mod_id}
mixins: [${common_mixins_file}]
dependencies:
  bootstrap:
    Eclipse:
      load: BEFORE
      required: true
      join-classpath: false
